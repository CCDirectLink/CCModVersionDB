- Create a `mods.json` file inside the config directory.
- Inside `mods.json` file, create an array of your mod paths (must be full path).

Example:
Single path
```js
[
    '/path/to/mod/'
]
```

Multiple paths
```js
[
    '/path/to/mod1/',
    '/path/to/mod2/',
    '/path/to/mod3/'
]
```